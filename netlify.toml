[build]
  command = "pip install -r requirements.txt && your-build-command"

[build.environment]
  PYTHON_VERSION = "3.13.1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.packages]
  python3-dev = true
  libcairo2-dev = true
  pkg-config = true